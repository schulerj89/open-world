{
  "name": "endless-low-poly-world",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run",
    "test:smoke": "playwright test",
    "capture:screenshots": "node scripts/capture-screenshots.mjs"
  },
  "dependencies": {
    "three": "latest"
  },
  "devDependencies": {
    "@playwright/test": "latest",
    "@types/node": "latest",
    "@types/three": "latest",
    "typescript": "latest",
    "vite": "latest",
    "vitest": "latest"
  }
}
